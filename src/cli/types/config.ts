export type Config = {
  registry: string;
  directories: {
    components: string;
    config: string;
    hooks: string;
    tailwind: string;
    types: string;
    utility: string;
  };
  aliases: {
    components: string;
    config: string;
    hooks: string;
    tailwind: string;
    types: string;
    utility: string;
  };
};
